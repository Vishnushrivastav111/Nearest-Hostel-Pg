{
  "name": "nearest-hostel-functions",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "ensure-presets": "npm run build && node lib/ensure-presets.js"
  },
  "dependencies": {
    "cloudinary": "^2.7.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
