export const PAGINATION = {
  publicPageSize: 12,
  adminPageSize: 20,
  dashboardLimit: 30,
} as const;

export const DUPLICATE_LEAD_WINDOW_MINUTES = 15;

export const INDIAN_MOBILE_PATTERN = /^[6-9]\d{9}$/;

export const SLUG_PATTERN = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;
